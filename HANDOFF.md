# HANDOFF — 租約產生器（Session 交接筆記）

> 最後更新：2026-06-03　|　分支：`dev`（主分支 `master`）
> 本筆記專指「租約產生器」這個新工具的進度；學區快查、房貸試算為既有功能，不在此範圍。

---

## 0. 待修改清單（2026-06-03 第二輪，使用者實測後回饋）

> 分批進行，逐項打勾，避免漏改。

**批次一：合約本文修正（rent.html，已做）**
- [x] A. 契約審閱頁分頁：PDF 已分頁，但**匯出 Word 沒分頁** → exportWord 需把 `.pgbreak` 轉成 Word 認得的分頁符。
- [x] B. 第一頁（契約審閱頁）：B1 字體放大到 14pt；B2 承租人、出租人**分兩行**。
- [x] C. 第五條：各費用「冒號後換行」，每個負擔選項**各自一行**（管理費、水、電、瓦斯、網路皆同）。
- [x] D. 第六條：改寫為「本契約有關稅費，依下列約定辦理：」(一)房地稅出租人負擔 (二)公證費 ___ 元整，□出租人／□承租人／■雙方平均／□其他 **各自一行**；新增「公證費金額」「其他」欄位。
- [x] E. 第二十一條：改用內政部原文（含保證人那句）。系統選同意/不同意；選「同意」時下方三項出現 ■，**不論勾選與否文字都完整呈現**。
- [x] F. 立契約書人：姓名（簽章）後，**身分證字號、戶籍地址、聯絡電話各自換行**；「統一編號」字樣改為「身分證字號」。

**批次二：附件一 現況確認書改版（已做）**
- [x] G. 依參考 Word 改為 **3 欄表格（項次｜內容｜備註說明）**，11 個項次帶回原範本完整內容與備註；**附屬設備併為第 12 項（最後一格）**，已取消原本獨立的附屬設備清單頁。備註固定文字放 `rent-data.js` 的 `CONFIRM_NOTES`（資料邏輯分離）。內容欄主要勾選由表單帶入，點交時才填的細項（修繕方式、月/季/年繳、獨立權狀等）比照紙本以靜態 □ 呈現供手勾。

**批次四：第二輪實測後微調（2026-06-03，已做）**
- [x] I. 「住宅租賃契約書」標題移到**第一頁中段**、置中、**42pt 加粗**（原本在第 2 頁本文開頭，已移除不重複）。⚠ 使用者有提「如我提供的範例」但圖未收到，目前用政府版常見排版，待對照範例再微調位置/字級。
- [x] J. 第五條每項費用（管理費/水/電/瓦斯/網路）皆新增「□其他 ___」選項（表單加 radio + `feeother_<key>` 欄位）。
- [x] K. 列印 PDF 時隱藏 `.toast`（原本「合約已產生，可列印或匯出」浮動提示會印在每頁擋住內容）。
- [x] L. Word 匯出邊界調寬：`@page` 改為 `margin:2.54cm 3.17cm`（上下 2.54、左右 3.17）。

**批次五：第三輪實測後微調（2026-06-03，已做）**
- [x] M. 「住宅租賃契約書」標題（42pt 加粗置中）位置改為**置於第一頁「出租人（簽章）」下方**（不再放中段）。
- [x] N. 現況確認書第 8 點「於產權持有前」的三個選項（□無／□知道／□不知道）改為**逐行排列**。
- [x] O. 現況確認書第 11 點管理費改為**逐行排列**：住宅管理費（月/季/年繳/其他各一行）→ 空行 → 停車位管理費（同）→ 空行 → 積欠管理費。住宅「其他」仍綁定表單 `mgmtFee`（有填→■其他＋內容，空白→□其他 ______）。

**批次三：現場照片批次上傳（已做）**
- [x] H. 表單新增「現場照片」批次上傳（多檔），縮圖牆預覽 + 可逐張移除；上傳後在本機以 canvas 縮至最長邊 1000px、轉 JPEG(0.82) 控制檔案大小；輸出時附在合約末（附件二 現場照片，雙欄網格），PDF 列印與 Word 匯出皆含圖（純前端 base64，不外傳）。`PHOTOS` 陣列每筆保留 `caption` 欄位（目前不填），日後要加每張說明文字可直接沿用。
  - ⚠ 待使用者驗證：base64 內嵌圖在 **Word(.doc)** 的相容性視 Word 版本而定（PDF 列印一定可顯示）；若 Word 不顯示圖，再評估改 MHTML 內嵌。

---

## 1. 目標（本 session 要完成什麼）

打造個人網站的**第三個工具：租約產生器**，與既有的「林口學區快查」「房貸試算」並列於同一站。

核心需求：
- 讓房仲本人與客戶能**自行填欄位產生住宅租賃契約**，不必每次改整份 Word。
- 產出可**列印成 PDF**、也可**匯出 Word**，版面比照現行紙本租約。
- **附屬設備**可勾選並填數量。
- （第二階段）**上傳辨識**：①身分證正反面自動帶入姓名／身分證字號／戶籍地址；②電傳／謄本自動帶入物件基本資料。

本 session 範圍：**完成第一階段「手動版產生器」**；OCR 屬第二階段，尚未動工。

---

## 2. 已完成（檔案 / 功能）

| 檔案 | 內容 |
|---|---|
| `rent/rent.html` | 主頁：結構化表單 + 產生合約 + 列印PDF + 匯出Word。沿用全站 `../style.css` 設計系統與導覽列。 |
| `rent/rent-data.js` | 資料檔：`EQUIPMENT`（附屬設備清單）、`SPECIAL_TERMS`（特約範本）、`CLAUSES`（第七～二十四條固定條文）、`BUILDING_TYPES`/`RENT_TYPES`/`PAYER`（下拉選項）。 |
| `.gitignore` | 新增：忽略 `rent/reference/`（含真實個資的參考租約），避免上傳。 |
| `rent/reference/` | 放客戶提供的參考租約 `忠孝苑租約(...).docx`（**含真實個資，已被 git 忽略**）。 |

**已實作功能**：
- 表單分區：契約基本、當事人、租賃標的（第一條）、租期/租金/押金（第二～四條）、費用負擔（第五～六條）、其他約定（第九/十三/十九/二十一條）、特別約定（第二十三條）、附件一現況確認書、附屬設備清單、簽約日期。
- 「產生合約預覽」：輸出第一～二十四條 + 附件一現況確認書（表格）+ 附屬設備清單，標楷體、含 ■/□ 勾選呈現。
- 「列印 / 存 PDF」：`window.print()` + `@media print` 只留合約。
- 「匯出 Word」：HTML 包成 `.doc`（`application/msword` + BOM），Word 可直接開、中文正常。
- 「載入範例」：以**假資料**一鍵填入（不含真實個資），方便驗證版面。
- 「回到編輯」、Toast 提示。

**記憶檔**（跨 session）：`memory/rent-generator-plan.md` 已記錄分階段計畫與個資注意事項。

---

## 3. 進行中（做到哪、有無改到一半）

- **目前沒有改到一半的檔案**。第一階段程式碼完整、可執行。
- **狀態：等使用者在瀏覽器實測版面**。使用者已用 `file://` 開啟頁面測試中（頁面無需伺服器即可完整運作）。
- 待使用者回饋後，再微調欄位順序、設備品項、條款文字以更貼近紙本。

---

## 4. 關鍵決策與限制

**已確定的技術選擇**
- 維持專案原則：**純前端、零建置、無框架**（HTML + CSS + 原生 JS）。
- **資料與邏輯分離**：設備/特約/條款/選項等放 `rent-data.js`，行為邏輯放 `rent.html`。
- **沿用全站設計系統**：`../style.css` 的 CSS 變數、`.panel`、`.btn`、`.site-nav`、`.hero`。
- **分階段**：先手動版，OCR 第二階段再做（使用者 2026-06-03 拍板）。
- PDF＝瀏覽器列印；Word＝HTML→`.doc`（皆零依賴，不引入打包工具）。
- 契約範本依據：**內政部 113.7.8 台內地字第11302639334號函修正「住宅租賃契約書」**。

**不要改動 / 要小心的部分**
- `rent/reference/` 內是**真實個資**（姓名、身分證字號 O開頭、電話、銀行帳號），**已 gitignore，切勿提交或在範例中亂填**。
- 範例資料（`loadSample()`）一律用假資料，不要放真實個資。
- 條款文字屬**法定範本**，非必要勿任意刪改，以免影響契約效力。
- 既有的學區（`index.html`/`linkou-data.js`）、房貸（`mortgage.html`/`mortgage-data.js`）邏輯**不要動**。

**OCR 方向（尚未拍板，傾向）**
- 傾向**純前端 Tesseract.js**（影像不外傳、維持零後端、合個資考量），辨識結果僅當「草稿」仍須人工核對。
- 雲端 OCR 會需 API 金鑰＋後端代理、有費用、個資上傳第三方 → 與「零建置」衝突，列為備案。

**踩過的雷**
- docx 文字抽取：正則 `<w:t[^>]*>` 會誤吃 `<w:tab>` 自閉合標籤；要用 `<w:t(?: xml:space="[^"]*")?>` 排除。
- 本機**無可用的 Node / Python**（`python.exe` 是 Windows 商店空殼，實際無法執行）→ 無法跑 JS 工具鏈，**只能用瀏覽器 `file://` 測試**。
- 試過用 PowerShell `HttpListener` 架本機伺服器，遇到 `localhost` 逾時 / 殭屍 listener 卡埠（http.sys 由 System PID 4 持有）。**已放棄，改用 `file://` 直接開**（本頁無 fetch/XHR，file:// 完全可用）。臨時腳本 `rent/_devserver.ps1` 已刪除。

---

## 5. 待辦（依優先順序）

1. **使用者實測版面回饋**：載入範例→產生合約，比對紙本租約，回報要調整的欄位／順序／文字。
2. **核對條款完整度**：第一階段為求精簡，部分有微調——第五條費用略去「(六)其他費用」、第四條(二)押金抵充條文略簡、附件一現況確認書收斂為重點列。**正式使用前應逐條與內政部原範本核對**，確認法律文字無遺漏。
3. **附屬設備清單增補**：確認 `EQUIPMENT` 品項齊全（目前涵蓋窗簾、冰箱、洗衣機、電視、沙發、餐桌椅、床架、衣櫃、吊衣櫃、廚房置物櫃、流理台、瓦斯爐、排油煙機、洗碗機、熱水器、濾水器、冷氣、冷氣遙控器、免治馬桶遙控器、監控設備、燈飾、天然瓦斯）。
4. **多名當事人**：目前出租人／承租人各**僅支援一名**；若有共有人或多名承租人，需擴充欄位（待評估）。
5. **導覽列三頁互通**：在 `index.html`、`mortgage.html` 導覽列加「租約產生器」連結——**使用者要求先暫緩**（貸款+租約仍在調整、尚未上架）。
6. **第二階段 OCR**：①身分證正反面 ②電傳/謄本，先與使用者決定辨識方式（傾向 Tesseract.js）再實作；務必保留人工核對。
7. **版面穩定後 git commit** 建立存檔點。

---

## 6. 環境

- **技術棧**：純前端 HTML + CSS + 原生 JavaScript，**無框架、無建置流程**。直接開檔即可執行。
- **租約工具目前用到的外部資源**：僅 Google Fonts（Noto Sans TC / Noto Serif TC，CDN）。合約本文字型優先 `標楷體 / DFKai-SB`（系統字型，無需下載）。
- **本工具目前無任何後端 / 外部 API 呼叫。**（全站其他頁另有用 Leaflet 1.9.4、OpenStreetMap、Nominatim，與本工具無關。）
- **第二階段 OCR 可能引入**：Tesseract.js（CDN，純前端）為傾向方案；雲端 OCR API 為備案（需金鑰＋後端，列為次選）。
- **平台 / 工具**：Windows 11、PowerShell；本機**無 Node、無可用 Python**。測試方式＝瀏覽器開 `file:///C:/repo/my-project/rent/rent.html`。
- **版本控管**：Git，目前分支 `dev`，主分支 `master`。

---

## 快速上手（給接手者）

```
# 直接用瀏覽器開（無需伺服器）
file:///C:/repo/my-project/rent/rent.html

# 改「設備 / 特約 / 條款 / 選項」→ 編輯 rent/rent-data.js
# 改「表單行為 / 產生合約 / 匯出」→ 編輯 rent/rent.html
# 參考紙本租約（含個資，勿提交）→ rent/reference/
```
